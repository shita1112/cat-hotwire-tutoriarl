<%= form_with(model: chick) do |form| %>
  <% if chick.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(chick.errors.count, "error") %> prohibited this chick from being saved:</h2>

      <ul>
        <% chick.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.number_field :age %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
